Explicação do Código

> Determinação do Tamanho do Bloco:
m = Math.sqrt(n).to_i calcula o tamanho do bloco, onde Math.sqrt(n) dá a raiz quadrada de n e to_i converte para um número inteiro.

> Iteração pelos Blocos:
O loop (0...n).step(m).each do |i| itera sobre os blocos, começando em i e saltando m posições a cada iteração.

> Verificação do Bloco:
if array[i + m - 1] >= item verifica se o item pode estar no bloco atual. Se o valor no final do bloco (array[i + m - 1]) é maior ou igual ao item, então o item pode estar neste bloco.

> Busca Linear no Bloco:
O loop interno (i...[i + m, n].min).each do |j| faz uma busca linear dentro do bloco atual para encontrar o item.

> Retorno do Índice:
Se o item for encontrado, o índice é retornado; se não, o loop continua até o final da lista.

>Item Não Encontrado:
Se o item não for encontrado após verificar todos os blocos, o método retorna -1.